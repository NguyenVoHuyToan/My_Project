import React from 'react'

function Home(props) {
  // const myData = props.data
  return (
   <div>Home
        {/* {myData.map((item,index) => {
          if(index < 5) {
            return (
              <div key={index}>{item}</div>
            )
          };
          return myData;
        })} */}
   </div>
    
  )
}

export default Home